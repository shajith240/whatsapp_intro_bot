{
  "name": "whatsapp-introduction-validator",
  "version": "1.0.0",
  "description": "WhatsApp bot that validates introduction messages from juniors",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/webhook-server.js",
    "dev": "nodemon src/webhook-server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:webhook": "node scripts/test-webhook.js",
    "setup": "node scripts/setup-webhook.js",
    "setup:configure": "node scripts/setup-webhook.js configure",
    "setup:verify": "node scripts/setup-webhook.js verify",
    "setup:test": "node scripts/setup-webhook.js test",
    "setup:profile": "node scripts/setup-webhook.js profile",
    "setup:instructions": "node scripts/setup-webhook.js instructions"
  },
  "keywords": ["whatsapp", "bot", "validation", "introduction"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.2",
    "body-parser": "^1.20.2",
    "crypto": "^1.0.1",
    "moment": "^2.29.4",
    "winston": "^3.11.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  }
}
